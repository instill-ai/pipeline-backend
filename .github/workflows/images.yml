name: Build and Push Images

on:
  release:
    types: [published]

jobs:
  docker-hub:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: dropletbot
          password: ${{ secrets.botDockerHubPassword }}
      - name: Build and push
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: instill/pipeline-backend:latest,instill/pipeline-backend:${{ github.ref_name }}
          cache-from: type=registry,ref=instill/pipeline-backend:buildcache
          cache-to: type=registry,ref=instill/pipeline-backend:buildcache,mode=max
